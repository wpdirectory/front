import { createAction } from '@reduxjs/toolkit'

export const settingsToggleNavigationExpanded = createAction('settings/TOGGLE_NAVIGATION_EXPANDED')

export const settingsToggleNavigationOpen = createAction('settings/TOGGLE_NAVIGATION_OPEN')
