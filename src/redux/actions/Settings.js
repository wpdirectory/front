import { createAction } from '@reduxjs/toolkit'

export const toggleNavigationExpanded = createAction('settings/TOGGLE_NAVIGATION_EXPANDED')

export const toggleNavigationOpen = createAction('settings/TOGGLE_NAVIGATION_OPEN')
